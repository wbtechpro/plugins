Manual

Step 1.
Add attached css file and Jquery 1.7 library to html document

Step 2.
Initialize plugin

$(document).ready(function(){
	wbtDragOverflow.init({
		selector: $(".any-selector"), //necessary. Selects element to drag
		cursor: "grab", //optional
		icon: true //optional
});